# 智答 AI — 项目改造报告

## 一、改造概述

**改造目标**：将原有的恋爱咨询聊天机器人改造为企业级知识库问答系统。

**项目名称**：智答 AI — 基于 RAG 的智能知识库问答平台

**核心技术栈**：Spring Boot 3.4.4 + Spring AI 1.0.0 + PgVector + Vue 3 + Vite

---

## 二、改造内容详情

### 阶段一：后端核心改造

#### 1. 依赖管理（pom.xml）

新增三个依赖：
- `spring-boot-starter-data-jpa`：JPA 数据持久化
- `pdfbox 3.0.1`：PDF 文档解析
- `poi-ooxml 5.2.5`：Word 文档解析

#### 2. 数据库设计（init.sql）

从 2 张表扩展为 5 张表：

| 表名 | 用途 |
|------|------|
| `kb_document` | 文档元数据（标题、文件名、类型、分类、处理状态、分块数） |
| `vector_store` | 文档分块向量存储，metadata 增加来源追溯字段 |
| `chat_session` | 聊天会话 |
| `chat_message` | 聊天消息（含来源追溯 sources JSONB 字段） |
| `query_log` | 问答审计日志 |

#### 3. 数据模型层（model 包，8 个新文件）

**实体类：**
- `KbDocument`：文档实体（映射 kb_document 表）
- `ChatSession`：会话实体
- `ChatMessage`：消息实体（含 sources 字段存储来源引用）

**枚举类：**
- `DocumentStatus`：文档处理状态（PROCESSING / READY / ERROR）
- `MessageRole`：消息角色（USER / ASSISTANT / SYSTEM）

**DTO 类：**
- `ChatRequest`：聊天请求参数
- `ChatResponse`：聊天响应结果
- `SourceReference`：来源引用信息（文档标题、文件名、匹配片段、相似度分数）

#### 4. 数据访问层（repository 包，3 个新文件）

- `KbDocumentRepository`：文档 CRUD + 按分类/状态查询
- `ChatSessionRepository`：会话 CRUD + 按更新时间排序
- `ChatMessageRepository`：消息按会话查询 + 级联删除

#### 5. 业务服务层（service 包，4 个新文件）

- `DocumentParser`：文档解析器（策略模式），支持 PDF、Word、TXT、Markdown 四种格式
- `KnowledgeBaseService`：知识库管理核心服务
  - 上传文档：保存文件 → 解析内容 → 文本分块 → 向量化 → 存入 PgVector
  - 删除文档：删除向量数据 + 磁盘文件 + 数据库记录
  - 文档列表/详情查询
- `ChatService`：核心聊天服务
  - 集成 RAG 检索增强生成
  - 集成数据库持久化会话记忆（20 轮上下文窗口）
  - 集成来源追溯（通过 SourceTracingDocumentRetriever）
  - 支持同步聊天和 SSE 流式聊天两种模式
- `ChatSessionService`：会话管理 CRUD（创建/重命名/删除/列表）

#### 6. 聊天记忆持久化

- `DatabaseChatMemoryRepository`：实现 Spring AI 的 ChatMemoryRepository 接口，将对话记忆持久化到 PostgreSQL，服务器重启后对话历史不丢失

#### 7. 控制器层（controller 包，2 个新文件）

**DocumentController**（`/knowledge/document/*`）：

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/upload` | 上传文档（MultipartFile） |
| DELETE | `/{id}` | 删除文档 |
| GET | `/list` | 文档列表（支持按分类过滤） |
| GET | `/{id}` | 文档详情 |

**ChatController**（`/chat/*`）：

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/session` | 创建会话 |
| GET | `/sessions` | 会话列表 |
| PUT | `/session/{id}` | 重命名会话 |
| DELETE | `/session/{id}` | 删除会话 |
| POST | `/send` | 同步聊天 |
| GET | `/stream` | SSE 流式聊天 |
| GET | `/history/{sessionId}` | 获取历史消息 |

#### 8. 配置更新

- 主应用入口：移除 DataSourceAutoConfiguration 排除，启用数据库连接
- `application.yml`：新增 JPA 配置（ddl-auto: update）、pgvector 配置、文件上传限制（50MB）
- `PgVectorVectorStoreConfig`：启用 PgVector 向量存储（HNSW 索引、1536 维、余弦距离）

#### 9. 旧代码清理

删除旧版垂直场景代码（共 6 个 Java 文件 + 3 个 Markdown 文档）：
- 旧版应用服务（垂直场景业务逻辑）
- 旧版文档加载器（场景知识库加载）
- 旧版内存向量存储配置
- 旧版云端 RAG 配置
- 旧版状态过滤工厂
- 旧版查询增强器
- `resources/document/` 下 3 个旧版知识库文档

`AiController` 精简为仅保留通用工具智能体端点。

---

### 阶段二：前端改造

#### 1. API 层重写（api/index.js）

替换旧版场景 API，新增 8 个接口方法：
- 文档管理：`uploadDocument`、`listDocuments`、`deleteDocument`
- 会话管理：`createSession`、`listSessions`、`renameSession`、`deleteSession`
- 聊天：`chatStream`（SSE 流式）、`getChatHistory`

#### 2. 路由改造（router/index.js）

| 路径 | 页面 | 说明 |
|------|------|------|
| `/` | KnowledgeBase | 主聊天页（默认页） |
| `/documents` | DocumentManager | 文档管理页 |

#### 3. 新增 6 个组件

| 组件 | 功能 |
|------|------|
| `Sidebar.vue` | 会话列表侧边栏（新建/切换/删除会话，链接到文档管理） |
| `MessageList.vue` | 消息列表（Markdown 渲染 + 来源引用卡片 + 流式打字效果） |
| `SourceCard.vue` | 来源引用卡片（显示文档名、匹配片段、相似度） |
| `ChatInput.vue` | 聊天输入框（Enter 发送、Shift+Enter 换行、自动调整高度） |
| `UploadZone.vue` | 拖拽文件上传区（支持 PDF/Word/TXT/MD） |
| `DocumentTable.vue` | 文档列表表格（名称、类型、分类、状态、操作） |

#### 4. 新增 2 个页面

- `KnowledgeBase.vue`：主聊天页，ChatGPT 风格布局——左侧会话列表 + 右侧聊天区域
- `DocumentManager.vue`：文档管理页——拖拽上传区 + 分类筛选 + 文档表格

#### 5. UI 主题升级

- 从赛博朋克风格改为简洁现代的 ChatGPT 风格
- 建立 CSS 变量体系（主色调、背景色、边框色、阴影等）
- 更新 `index.html` SEO 信息为「智答 AI」

#### 6. 新增依赖

- `marked`：Markdown 内容渲染
- `highlight.js`：代码块语法高亮
- `dompurify`：HTML 安全过滤（防 XSS）

#### 7. 旧代码清理

删除 5 个文件：
- `Home.vue`（赛博朋克首页）
- 旧版垂直场景页
- 旧版通用智能体页
- `HelloWorld.vue`、`AppFooter.vue`（旧组件）

---

### 阶段三：RAG 来源追溯

#### 1. 来源追踪检索器（SourceTracingDocumentRetriever）

包装 Spring AI 的 `VectorStoreDocumentRetriever`，在每次检索后自动将命中的文档存入 ThreadLocal，供后续处理使用。

#### 2. 来源上下文持有器（SourceContextHolder）

基于 ThreadLocal 的文档持有器，在单次请求的生命周期内传递来源信息，请求结束后清除。

#### 3. RAG 配置工厂（KnowledgeBaseRagConfig）

创建带来源追踪的 `RetrievalAugmentationAdvisor`，支持：
- Top K 结果数量配置（默认 5）
- 相似度阈值配置（默认 0.5）
- 按知识库分类过滤

#### 4. ChatService 集成

- 使用 `RetrievalAugmentationAdvisor` 替代原有 `QuestionAnswerAdvisor`
- 同步聊天：从 ThreadLocal 提取来源文档，构建 SourceReference 列表返回给前端
- 流式聊天：同样经过 RAG Advisor，来源信息在流式完成后保存到数据库
- 来源信息使用 Jackson 序列化为 JSON 存储

---

## 三、文件变更统计

| 阶段 | 新增文件 | 修改文件 | 删除文件 | 代码行变更 |
|------|---------|---------|---------|-----------|
| 阶段一（后端） | 14 | 5 | 9 | +1,352 / -615 |
| 阶段二（前端） | 8 | 5 | 5 | +1,472 / -1,521 |
| **合计** | **22** | **10** | **14** | **+2,824 / -2,136** |

---

## 四、Git 提交记录

| 提交哈希 | 说明 |
|---------|------|
| `8172545` | Phase 1：从恋爱咨询机器人改造为企业知识库问答系统（后端） |
| `57f29bd` | Phase 2-3：前端 ChatGPT 风格 UI 改造 + RAG 来源追溯 |
